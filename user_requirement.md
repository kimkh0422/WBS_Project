# 사용자 요구사항 명세서 (URS)

> **누가, 무엇을, 왜** 할 수 있어야 하는지 정의합니다. 구현·DB·API 세부는 [`system_requirement.md`](./system_requirement.md)를 따릅니다.

---

## 1. 제품 개요

| 항목 | 내용 |
|------|------|
| **제품명(기본)** | 지엠티 프로젝트 매니저 (표시 제목은 설정에서 변경 가능) |
| **목적** | 여러 프로젝트의 WBS(작업 분해)를 한곳에서 관리하고, 일정·담당·진행을 여러 방식으로 보고, 팀과 공유하며, 필요 시 서버에 맞춰 두고, AI·주간보고로 업무를 돕는다. |
| **주요 사용자** | 프로젝트/과제 담당자, 팀 리더, (선택) 시스템 관리자 |
| **UI 언어** | 한국어 |
| **접근** | 웹 브라우저(반응형) |

---

## 2. 가입·로그인·이용 조건

### UR-AUTH-01 회원가입·로그인
- 이메일과 비밀번호로 가입·로그인할 수 있다. (비밀번호는 일정 길이 이상)
- Google·GitHub 계정으로 로그인할 수 있다.

### UR-AUTH-02 가입 직후 경험
- 가입 직후에도 **앱은 바로 쓸 수 있어야** 한다. (로컬에서 프로젝트·작업 생성·편집 가능)
- **관리자 승인 전**에는 팀 서버(DB)와의 **동기화(업로드/다운로드)는 할 수 없다**는 안내가 있어야 한다.

### UR-AUTH-03 승인 후
- 관리자 승인을 받은 사용자는 **서버와 데이터를 맞출 수 있다**(협업·다른 기기 반영).
- 승인된 사용자는 **등록된 모든 프로젝트 목록을 볼 수 있고**, 타인 프로젝트의 작업 내용도 **읽기**할 수 있다(구현 정책상). 다만 **편집**은 초대·멤버로 권한이 있을 때만 가능해야 한다.

### UR-AUTH-04 관리자
- 관리자는 회원 목록·승인·일부 계정 관리(삭제 등)를 할 수 있어야 한다.
- 관리자 모드는 **앱 비밀번호** 등으로 보호될 수 있다.

---

## 3. 프로젝트

### UR-PROJ-01 생명주기
- 프로젝트를 **생성·이름/설명·기간** 등을 수정·삭제할 수 있다.
- 프로젝트를 **복사**하면 소속 작업이 포함된 새 프로젝트가 생기고, 복사본은 **나의 소유**로 둔다.

### UR-PROJ-02 기간 규칙
- 프로젝트에 시작·종료 기간을 두면, **그 범위를 벗어나는 작업 일정은 허용하지 않는다.**

### UR-PROJ-03 투입·보고 메타
- 프로젝트 단위로 **투입 인원·투입 비율(%), 월별 투입**을 둘 수 있다.
- **최소 작업 공수(일)** 기준을 두어 WBS 세분화에 참고할 수 있다.
- **주간보고용** 필드(분류, 주관기관, 예산, 기간 표기, 과제명 약어/전체 등)를 둘 수 있다.

---

## 4. 작업(WBS)

### UR-TASK-01 구조
- 작업은 **트리(상위·하위)** 구조로 둔다.
- WBS 번호(레벨별 접두 규칙)로 식별한다.

### UR-TASK-02 속성
- 작업마다 **이름, 시작일·종료일, 진행률, 담당자, 상태, 공수(일), 설명, 산출물, 체크리스트**를 둘 수 있다.
- **선행 작업(의존성)**을 지정하면, 일정이 그 관계를 반영하도록 **자동 조정**되어야 한다.
- **마일스톤**(시점)·**이슈** 표시를 켤 수 있다.
- **베이스라인**(기준 일정·공수)을 저장하고, 현재 일정과 **차이를 비교**할 수 있다.

### UR-TASK-03 편집 조작
- 작업 **추가·삭제**, **들여쓰기/내어쓰기**, 순서 **위/아래·드래그**로 바꿀 수 있다.
- 여러 작업을 선택해 **일괄** 들여쓰기/내어쓰기·속성 반영(안전한 경우)을 할 수 있다.

### UR-TASK-04 과부하·일정
- 인원 투입과 공수를 바탕으로 **과부하(일정 상 충돌)**를 파악할 수 있고, **기간 연장** 또는 **투입율 조정**으로 완화할 수 있다.
- 프로젝트 일정을 **선행 관계에 맞게 한 번에 재계산**할 수 있다.

### UR-TASK-05 AI 연동
- 사용자가 지정한 필드는 **AI가 덮어쓰지 않도록** 잠글 수 있다.

---

## 5. 화면(뷰)

사용자는 동일 데이터를 아래 **뷰**로 전환해 볼 수 있어야 한다.

| ID | 뷰 | 사용자 가치 |
|----|-----|-------------|
| UR-VIEW-01 | **목록** | 계층·WBS 코드 중심으로 빠르게 훑기 |
| UR-VIEW-02 | **표** | 셀 단위로 대량 편집 |
| UR-VIEW-03 | **간트** | 시간축·의존성·베이스라인·(옵션) 크리티컬 패스 |
| UR-VIEW-04 | **칸반** | 상태별 보드, 드래그로 상태 변경, 카드에서 이름 수정/삭제 |
| UR-VIEW-05 | **마인드맵** | 가지 형태, 노드 편집·이동·확대/축소 |
| UR-VIEW-06 | **대시보드** | 프로젝트별·전체 통계, 마일스톤, (관리자) 방문 요약 |
| UR-VIEW-07 | **프로젝트** | 프로젝트 카드/목록 관리 |
| UR-VIEW-08 | **투입현황** | 프로젝트별·인원별 투입%, 담당자 이름 일괄 변경 |

---

## 6. 탐색·필터·정렬

### UR-NAV-01 프로젝트 선택
- **한 프로젝트만** 또는 **여러 프로젝트를 동시에** 선택해 작업을 볼 수 있다.

### UR-NAV-02 필터
- 상태, 담당자, 기간, **마일스톤만**, **이슈만**, WBS **레벨**, **기한 지난 미완료만**, **이번 주 완료만**, **담당 미배정만** 등으로 좁힐 수 있다.

### UR-NAV-03 정렬
- WBS·컬럼 기준 **오름차순/내림차순** 정렬이 가능하다.

---

## 7. 설정(사용자 맞춤)

- 앱 표시 제목, WBS **레벨 접두어·최대 깊이**를 바꿀 수 있다.
- **상태** 이름·색·해당 상태일 때의 **진척도(%)**를 정의할 수 있고, 기존 작업 진척도를 상태에 맞게 **일괄 맞출 수 있다.**
- 표 **컬럼 표시/숨김**, **줄바꿈**, **열 너비**, 간트 **크리티컬 패스 표시** 등을 설정할 수 있다.

---

## 8. 협업

### UR-COLLAB-01 역할
- 프로젝트마다 멤버에게 **소유/편집/보기** 역할을 줄 수 있다.

### UR-COLLAB-02 초대
- **초대 링크**를 만들고, 역할·유효 기간을 정할 수 있다. 링크로 참여한 사람은 멤버가 된다.

### UR-COLLAB-03 접근 요청
- 내가 목록에서 볼 수 있지만 **편집 권한이 없는** 프로젝트에 대해, **보기/편집 권한 요청**을 보낼 수 있다. 소유자·관리자가 승인/거절한다. 거절 후 **다시 요청**할 수 있다.

### UR-COLLAB-04 동시 작업 인지(선택)
- 설정이 켜진 환경에서는 **같은 프로젝트를 보고 있는 다른 사람**을 표시할 수 있다.

---

## 9. 데이터 맞추기·백업

### UR-DATA-01 동기화
- 승인된 사용자는 **한 번의 동작**으로 로컬 변경을 서버에 반영하고, 서버 최신을 다시 받아 **맞출 수 있다.**
- 진행 중임을 **진행률·단계 문구**로 알 수 있다. 끝나면 **몇 건이 올라갔는지/내려왔는지** 요약을 본다.

### UR-DATA-02 오프라인·로컬
- 서버 연동이 없거나 승인 전에도 **브라우저에 저장된 데이터**로 계속 작업할 수 있다.

### UR-DATA-03 가져오기·보내기
- **Excel**로 작업표를 가져오기(미리보기 후 반영)·보내기 할 수 있다.
- **JSON 전체 백업**을 파일로 받고, 다시 넣거나 **여러 백업을 합칠** 수 있다.
- **Markdown**으로 요약보내기 할 수 있다.
- 보내기 범위는 **전체 프로젝트** 또는 **선택한 프로젝트**다.

### UR-DATA-04 초기화
- **전체 초기화** 시 모든 프로젝트·작업이 지워지고, 빈 프로젝트 하나로 시작한다.

---

## 10. AI 보조

- 별도 **API 키**를 넣으면(사용자 기기에 저장) 다음을 할 수 있다: **새 WBS 초안 생성**, **기존 작업 재분석**, **의존성 자동 추론**.
- 텍스트와 **파일 첨부**를 넣을 수 있다.
- 결과는 **미리보기 후** 프로젝트에 **합치거나 대체**한다.
- WBS 교정 지시(프롬프트)를 **사용자가 수정·저장**할 수 있다.

---

## 11. 주간보고

- **기준 주(월요일 시작)**를 고른다.
- **내 담당만** 또는 **전체**, 프로젝트는 **전체 또는 여러 개 선택**한다.
- 해당 주와 겹치는 작업을 **요약 텍스트**로 만들고 **복사**한다.
- 프로젝트 메타(주간보고 필드)가 요약에 반영된다.
- 요약 중 작업을 **바로 열어 수정**할 수 있다.

---

## 12. 감사·이력

- (권한 있는 사용자는) 프로젝트에서 **누가 언제 무엇을 생성·수정·삭제했는지** 기록을 시간순으로 볼 수 있다.

---

## 13. 사용성·비기능(사용자 관점)

- 메뉴·도움말은 **한글**로 이해할 수 있어야 한다.
- **실행 취소/다시 실행**, **단축키 안내**, **토스트 알림**이 있어야 한다.
- **앱 버전·변경 이력**을 볼 수 있다.
- 새로고침 후에도 **작업 중이던 데이터가 유지**된다(로컬 또는 동기화된 서버 데이터).
- 로고·푸터(라이선스·버전) 등 브랜딩 영역이 있다.

---

## 14. 문서 관계

| 문서 | 내용 |
|------|------|
| **본 문서 (URS)** | 사용자 목표·기능·제약 (검수·기획용) |
| [**system_requirement.md**](./system_requirement.md) | 테이블·RLS·RPC·동기화 단계·로컬 키·Edge 계약·단축키·체크리스트 (**동일 구현 시 필수**) |

### 동일 프로그램을 문서만으로 만들 때

- **URS만으로는 불충분**합니다. 반드시 **SRS**와 **`supabase/migrations` 전체 순차 적용**, 그리고 **스케줄/AI/UI 세부는 소스 코드**(`src/lib/schedule.ts`, `workload.ts`, 각 모달·뷰)를 함께 봐야 합니다.
- SRS §20에 “문서 한계”를 명시해 두었습니다.

---

*구현 세부의 정본은 저장소 소스 및 `supabase/migrations`입니다.*
